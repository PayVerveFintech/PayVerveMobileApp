import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    personalInfoProgress: {
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center",

    },

    personalInfoProgressItem: {
        width: 108,
        justifyContent: "center",
        gap: 8,
        alignItems: "center",
    },
})