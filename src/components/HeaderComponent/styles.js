import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    header: {
        flexDirection: "row",
        alignItems: "center",
        marginVertical: 30,
        justifyContent: "center",
        position: "relative"
    },

    title: {
        fontSize: 22,
        fontWeight: "700"
    },
})