import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    boxWrapper: {
        gap: 8
    },

    boxLabel: {
        fontSize: 16,
    },

    inputBox: {
        borderColor: "#101820",
        borderWidth: 1,
        padding: 10,
        borderRadius: 4,
        height: 46,
    },
})