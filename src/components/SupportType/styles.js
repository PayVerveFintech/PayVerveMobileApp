import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    supportType: {
        flexDirection: "row",
        gap: 18,
        alignItems: "center",
        padding: 12,
    },

    supportDesc: {
        fontSize: 18
    }
})