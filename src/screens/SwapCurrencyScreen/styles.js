import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
        backgroundColor: "#FFF"
    },

    formView: {
        gap: 12,
    },

    buttonView: {
        marginTop: 60,
        alignItems: "center"
    },

    touchableText: {
        fontSize: 12,
        color: "#2196F3"
    },
})